# Sprint S38 — File Upload + Document Tracking UI

> **Status**: `completed`
> **Prerequisite**: S37
> **Phase**: 4 (Project Nexus)

---

## Sprint Goal

> Add file upload UI to deal detail (Google Drive link + device file), NDA/MOU document tracking page, and file AI parse status display.

---

## Stage 1: Sprint Planning

### User Stories

- [x] **US-1**: File upload modal on deal detail (link or device file)
- [x] **US-2**: File parse status display per file
- [x] **US-3**: NDA/MOU document tracking page with expiry warnings

### Definition of Done

- [x] **DoD-1**: File upload modal works on `/deals/[id]`
- [x] **DoD-2**: Parse status shown per file (pending/parsed/error)
- [x] **DoD-3**: `/documents` lists all NDA/MOU with client name, type, status, dates
- [x] **DoD-4**: Expiry warning badges (amber ≤30 days, red = expired)
- [x] **DoD-5**: Navigation includes documents entry
- [x] **DoD-6**: Build passes with zero errors

---

## Stage 2: Implementation

- File upload modal integrated into deal detail page
- Parse status indicator on file cards
- New `/documents` page with:
  - Summary cards (total, signed, needs attention)
  - Full document list with client name, doc type, status
  - Expiry badges: red for expired, amber for ≤30 days, green otherwise
- Backend: added `get_all_documents()` service function, updated router to return all docs when no `client_id` filter
- Frontend: added `documents` namespace to `nxApi` with `listAll`, `listByClient`, `expiring`, `update`
- Desktop sidebar: added "文件追蹤" nav item with FileCheck icon

---

## Stage 3: Review & DoD Verification

| DoD | Status | Evidence |
|-----|--------|----------|
| DoD-1 | PASS | Upload modal on deal detail |
| DoD-2 | PASS | Parse status shown on file cards |
| DoD-3 | PASS | `/documents` page lists all NDA/MOU |
| DoD-4 | PASS | Expiry badges render correctly |
| DoD-5 | PASS | Sidebar includes "文件追蹤" link |
| DoD-6 | PASS | `npm run build` zero errors |

---

## Stage 4: Retro & Refactor

### What went well
- Backend already had the document endpoints, just needed a "list all" function
- Documents page design consistent with existing card patterns

### What to improve
- Consider inline status update on documents page (currently read-only list)
- File upload should eventually support actual file storage (currently link-based)

### Tech debt noted
- `get_all_documents()` uses `NULLS LAST` — verified works with SQLite adapter
