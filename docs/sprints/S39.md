# Sprint S39 — Push Dashboard + Global Search

> **Status**: `completed`
> **Prerequisite**: S38

### Sprint Goal

> Add push dashboard (home page showing deals needing push, pending reminders, today's meetings, stale TBDs), global cross-entity search, and navigation updates.

### Deliverables

1. **Push Dashboard** (`/dashboard`) — Home page with:
   - Summary card: total action items count
   - Today's meetings section
   - Deals needing push (idle >7 days)
   - Pending reminders
   - Stale TBD items
   - Quick action buttons (新增情報 / 商機列表)

2. **Global Search** (`/search`) — Cross-entity keyword search:
   - Backend: `services/nexus/search.py` + `backend/routers/nexus/search.py`
   - Searches across deals, clients, partners, contacts, intel
   - Filter tabs by entity type
   - Frontend: search input with enter-to-search

3. **Navigation updates**:
   - Home redirect changed from `/deals` to `/dashboard`
   - Desktop sidebar: added 控制台 + 搜尋 links
   - Dashboard top bar: search icon links to `/search`

4. **API client**: Added `SearchResults` type and `nxApi.search()` method
